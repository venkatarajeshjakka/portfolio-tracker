import { withStyles } from "@material-ui/core/styles";
import { Typography } from "@material-ui/core";

const GreenTextTypography = withStyles({
  root: {
    color: "#32cd32"
  }
})(Typography);

export { GreenTextTypography };
